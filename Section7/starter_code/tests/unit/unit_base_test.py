from app import app
from unittest import TestCase


class UnitBaseTest(TestCase):
    pass

# create this file only for imports, so we create only once unused imports instead of creating in every unit test file
